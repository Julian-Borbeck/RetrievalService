name: retrieval
channels:
  - defaults
dependencies:
  - streamlit
  - requests
  - jupyter
  - networkx
  - tqdm
  - scipy
  - fastapi
  - pip:
    - chromadb
    - pydantic
    - filelock
prefix: /opt/miniconda3/envs/retrieval
